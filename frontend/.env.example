# ==========================================
# Frontend Environment Variables (.env)
# ==========================================

# The URL of your deployed Spring Boot backend API.
# For local development, you can leave this blank or point it to localhost.
# Because we are using Netlify Proxy rules in netlify.toml,
# this should just be set to /api in your Netlify dashboard!
VITE_API_BASE_URL=/api
